/**
 * Copy this file to keys.js and set secrets there.
 * Add keys.js to .gitignore if you use version control — do not commit real keys.
 *
 * Master key: unlocks every user’s submissions.
 * userKeys: map of numeric user id (string) → that person’s key.
 */
window.SCRIPTOR_KEYS = {
  masterKey: "",
  userKeys: {
    // "1669": "",
    // "1514": "",
  }
};

/* ─── Optional: copy the block below into keys.js for local testing only ───
window.SCRIPTOR_KEYS = {
  masterKey: "scriptor-demo-master",
  userKeys: {
    "1669": "scriptor-demo-user-1669"
  }
};
*/
